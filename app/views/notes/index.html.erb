
<%= link_to "new_note", new_note_path, data: { turbo_frame: 'modal'} %>

<h1><%= current_user.name %> のNote一覧</h1>


<div id="notes">
  <% if @notes.present? %>
      <% @notes.each do |note| %>
        <%= render note %>
      <% end %>
  <% end %>
</div>

